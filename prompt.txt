I need your help to fix an existing "saveIMOProcess" API integration.

Context

"saveIMOProcess" is already integrated in the application, but the current implementation is missing a few fields/objects from the expected payload.

I will share the related files/code with you. Please inspect the complete flow carefully before making any changes.

There are already other related API integrations in the same flow:

- "GetSpeciality"
- "GetAvailableDBQ"
- "SelectedDBQIsMO"

These integrations are already working correctly and their payload/implementation should be treated as the reference for the existing flow.

Important Scope

We only need to fix "saveIMOProcess".

Do NOT unnecessarily modify:

- "GetSpeciality"
- "GetAvailableDBQ"
- "SelectedDBQIsMO"
- Existing working API integrations
- Unrelated UI/business logic

If any existing code from these APIs is required as a reference, analyse it, but don't change it unless there is a direct dependency that absolutely requires a change.

What I need you to do

Once I share the files:

1. Understand the existing flow first
   
   - Identify where "saveIMOProcess" is called.
   - Identify the data collected from the UI/state/form.
   - Identify how the request payload is currently constructed.
   - Identify the API/service method responsible for calling "saveIMOProcess".
   - Identify the response handling.
   - Identify how the saved data is eventually persisted/displayed.

2. Compare the current implementation with the expected payload
   
   - I will provide the correct/expected payload.
   - Compare it field-by-field with the payload currently generated by the application.
   - Clearly identify:
     - Missing fields
     - Missing nested objects
     - Incorrect field names
     - Incorrect values
     - Incorrect nesting
     - Incorrect data types
     - Fields that are being sent but should not be sent
   - Do not assume a field mapping if the source of that value is unclear.

3. Trace every missing field
   For each missing field, determine:
   
   - Where the value should come from.
   - Whether it already exists in component state/form data/API response.
   - Whether it needs to be mapped from an existing object.
   - Whether another API response already provides the required value.
   - Whether the backend expects the field to be stored in a specific table/entity.

4. Check the DB/table persistence flow
   The "saveIMOProcess" data should ultimately be stored correctly in the relevant database/table.
   
   Please trace:
   "UI/Form → State/Data → saveIMOProcess Payload → API → Backend Model/DTO → Database/Table"
   
   Identify if any field is getting lost at any point in this flow.
   
   If the frontend payload is correct but the backend/database mapping is missing, clearly point that out instead of modifying the frontend blindly.

5. Use the existing working APIs as references
   Analyse "GetSpeciality", "GetAvailableDBQ", and "SelectedDBQIsMO" to understand:
   
   - Existing data structures
   - IDs and relationships
   - How speciality/DBQ/IMO-related data is represented
   - Existing naming conventions
   - Existing mapping patterns
   
   But remember: the actual fix should be limited to "saveIMOProcess" unless a dependency makes another change unavoidable.

6. Do not guess
   If you cannot determine where a required value comes from, stop and ask me for the specific file/API response/model you need.
   
   For example:
   
   - If the backend DTO is required, ask me for it.
   - If the DB model/entity is required, ask me for it.
   - If an API response is required to determine an ID mapping, ask me for the response.
   - If a service/helper file is referenced but not provided, ask me for that file.
   
   Do NOT invent field mappings or hardcode values just to make the payload match.

Expected output from you

Before changing anything, explain:

1. Current implementation

- How "saveIMOProcess" currently works.

2. Expected implementation

- What the correct flow should be.

3. Payload comparison
Create a clear comparison:

Field| Expected Payload| Current Payload| Status| Source/Mapping

4. Root cause
Explain exactly why the fields are missing.

5. Required changes
List the minimum files/functions that need to be changed.

6. Implementation
Then provide the code changes required to fix "saveIMOProcess".

7. DB persistence
Explain how each newly added field reaches the required backend/table and confirm whether any backend change is required.

8. Regression check
Confirm that:

- "GetSpeciality" remains unchanged.
- "GetAvailableDBQ" remains unchanged.
- "SelectedDBQIsMO" remains unchanged.
- Existing working behaviour is preserved.
- Only the required "saveIMOProcess" flow is modified.

Important

Do not start rewriting code immediately.

First analyse the files I provide and tell me if you have enough information.

If any required file, API contract, backend model, expected response, or database mapping is missing, ask me for it first.

The main objective is:

«Make "saveIMOProcess" generate and persist the complete expected payload correctly, while keeping the already-working "GetSpeciality", "GetAvailableDBQ", and "SelectedDBQIsMO" integrations untouched.»
